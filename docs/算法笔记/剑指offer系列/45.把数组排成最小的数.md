## 题目

输入一个非负整数数组，把数组里所有数字拼接起来排成一个数，打印能拼接出的所有数字中最小的一个。

## 代码

`TODO`注解

```java
class Solution {
    public String minNumber(int[] nums) {
        int len = nums.length;
        String[] strs = new String[len];
        for(int i = 0; i < len; i++){
            strs[i] = String.valueOf(nums[i]);
        }
        quickSort(strs,0,len - 1);
        StringBuffer ans = new StringBuffer();
        for(String str:strs){
            ans.append(str);
        }
        return new String(ans);
    }
    public void quickSort(String[] strs, int l, int r){
        if(r <= l) return;
        int i = l, j = r;
        String tmp = strs[i];
        while(i < j){
            while(i < j && (strs[j] + strs[i]).compareTo(strs[i] + strs[j]) >= 0) j--;
            while(i < j && (strs[i] + strs[j]).compareTo(strs[j] + strs[i]) <= 0) i++;
            tmp = strs[i];
            strs[i] = strs[j];
            strs[j] = tmp;
        }
        strs[i] = strs[l];
        strs[l] = tmp;
        quickSort(strs, l, i-1);
        quickSort(strs, j+1, r);
    }
}
```